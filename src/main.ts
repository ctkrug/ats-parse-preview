const app = document.querySelector<HTMLDivElement>("#app");

if (app) {
  app.innerHTML = `
    <main>
      <h1>ATS Parse Preview</h1>
      <p>Scaffold running. Upload UI and parsers land in the next build pass.</p>
    </main>
  `;
}
